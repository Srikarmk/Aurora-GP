# Pinned to the versions the reported results were produced with
# (macOS arm64, Python 3.13.11). Newer versions will likely work.
numpy==2.4.2
scipy==1.17.1
scikit-learn==1.8.0
pandas==2.3.3
matplotlib==3.10.8
seaborn>=0.13

# Exact GP baseline
torch==2.10.0
gpytorch==1.15.2

# Data loading (original dataset preparation only)
openpyxl>=3.1.0
xlrd>=2.0.0
